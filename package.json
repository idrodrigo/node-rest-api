{
  "name": "todo-node-api",
  "version": "1.0.0",
  "description": "todo-server-rest-api-node",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=development node --watch src/index.js",
    "start": "cross-env NODE_ENV=production node src/index.js",
    "build": "cd client && npm install && npm run build",
    "client": "cd client && npm run dev"
  },
  "author": "Rho",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.1",
    "mongoose": "^7.4.0",
    "morgan": "^1.10.0",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "cross-env": "^7.0.3",
    "dotenv": "^16.3.1"
  },
  "eslintConfig": {
    "extends": "standard"
  }
}